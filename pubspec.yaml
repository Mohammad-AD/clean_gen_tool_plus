name: clean_gen_tool
description: CleanGenTool is a Dart package designed to help developers create and manage clean architecture projects efficiently. It provides a command-line interface to generate boilerplate code, ensuring a consistent structure across your Dart applications.
version: 1.0.6
repository: https://github.com/Mohammad-AD/clean_gen_tool

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  path: ^1.9.1
  args: ^2.7.0
  yaml: ^3.1.3
  io: ^1.0.5

dev_dependencies:
  lints:
  test:

executables:
  clean_arch: clean_gen_tool